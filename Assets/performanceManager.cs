using System.Collections;
using System.Collections.Generic;
using UnityEngine;

using UnityEngine.UI;

public class performanceManager : MonoBehaviour
{
    // Start is called before the first frame update
    /*void Start()
    {
        instance = GetComponent<performanceManager>();
    }*/

    //public static performanceManager instance;
    /*public static performanceManager getInstance()
    {
        if(instance == null)
        {
            instance = GameObject.Find("PERFORMANCE").GetComponent<performanceManager>();
        }

    }*/
    /* Shop - defi - online - ranking - parametres - infos
    public Image[] menuPRaycasts;
    private List<Image> renableRaycasts; // enregistre les raycast à reactiver quand ferme sous menu

    public void openSousMenuP()
    {
        /*renableRaycasts = new List<Image> { };
        // on désactive tout les raycasts target de menuP
        foreach(Image i in menuPRaycasts)
        {
            if (i.raycastTarget && i.gameObject.activeInHierarchy)
            {
                i.raycastTarget = false;
                renableRaycasts.Add(i);
            }
        }
    }*/

    /*public void closeSousMenuP()
    {
        foreach (Image i in renableRaycasts)
            i.raycastTarget = true;
    }*/
}
